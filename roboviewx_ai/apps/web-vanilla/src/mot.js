/**
 * Stub MOT (Motion) client - no-op when MOT service is not used.
 * Replace with real implementation when MOT backend is available.
 */
export class MotClient {
    constructor(_wsUrl) {}

    init(_overlayEl, _videoEl) {
        // No-op
    }
}
